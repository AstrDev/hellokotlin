<resources>

	<!-- Base application theme. -->
	<style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
		<!-- Customize your theme here. -->
		<item name="colorPrimary">@color/global_color_primary</item>
		<item name="colorPrimaryDark">@color/global_primary_dark_color</item>
		<item name="colorAccent">@color/global_accent_color</item>
		<item name="android:windowBackground">@color/global_window_background_color</item>

	</style>

	<style name="ToolbarTheme" parent="ThemeOverlay.AppCompat.ActionBar">
		<item name="android:textColorPrimary">@color/global_toolbar_title_color</item>
	</style>
</resources>
